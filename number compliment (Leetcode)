#include <iostream>

using namespace std;

int
main ()
{
  int num, mask = 0;
  cin >> num;
  

  while (mask < num)
    {
      mask = (mask << 1) | 1;

    }

  int ans = (~num) & mask;

  cout<<"Answer : " << ans;

  return 0;
}
